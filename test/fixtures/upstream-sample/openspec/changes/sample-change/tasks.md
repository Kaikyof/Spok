# Tasks

## 1. Endpoint

- [x] 1.1 Add handler and verify the unit test passes
- [ ] 1.2 Wire route and verify `GET /greet` returns 200
* [ ] 1.3 Document the endpoint and verify the example in docs runs
