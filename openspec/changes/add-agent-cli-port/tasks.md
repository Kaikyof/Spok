## 1. Порт и адаптер Claude

- [ ] 1.1 `AgentCli` в `domain/repositories`; `ClaudeCodeCli` — текущий `AgentCliSource` за портом; `SessionsBloc` работает через порт; все тесты сессий зелёные, поведение на avtoto не изменилось
- [ ] 1.2 Общий `AgentEvent` (текст, инструмент, ошибка, итог, сырой) и тест парсера Claude на образцах `stream-json`

## 2. Другие адаптеры

- [ ] 2.1 `CodexCli`: `codex exec --json`, парсер JSONL на образцах, поддержка модели; тест «Codex вместо Claude Code»
- [ ] 2.2 `GeminiCli`: `gemini -p … --output-format json`; парсер; тест
- [ ] 2.3 `OpenCodeCli`: `opencode run --format json`; парсер; тест
- [ ] 2.4 `CursorCli` без headless: `invocationStyle: flat`, `start` не поддерживается; тест «Cursor» — «Скопировать команду»

## 3. Выбор и форма вызова

- [ ] 3.1 Определение инициализированных агентов по каталогам из таблицы upstream; селектор агента рядом с моделью и effort; сохранение по проекту; тест «проект для Claude и Codex»
- [ ] 3.2 Форма вызова в палитре по `invocationStyle` агента и таблице команда ↔ скилл; тест «та же команда, разные агенты»
- [ ] 3.3 Экран «Сессии» без инструментов: объяснение и ссылки на установку; локализация; тест «ни одного инструмента»
- [ ] 3.4 Список моделей по агенту (`SESSION_MODELS_<agent>`), параметры под полем показываются по `supports*`; тест
