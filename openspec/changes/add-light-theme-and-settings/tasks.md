## 1. Тема

- [ ] 1.1 Вторая палитра `AppColors` (по макету обеих тем), `AppTheme.light()`, `ThemeMode.system` по умолчанию; виджет-тесты «светлая система», «зафиксированная тёмная»
- [ ] 1.2 Тест контраста таблицы группы и карточки в обеих темах (`textContrastGuideline`); скриншоты обеих тем для README

## 2. Настройки

- [ ] 2.1 `settings_screen.dart` с полями тема, язык, агент и путь, модели, `git pull`, каталог данных; запись через `AppConfigSource` с сохранением чужих ключей; тесты «смена языка», «ручной ключ в файле»
- [ ] 2.2 Пункт настроек в навигации или в меню чипа проекта (по макету); локализация

## 3. Первый запуск

- [ ] 3.1 `SetupScreen`: состояние «каталог без OpenSpec» с предложением `openspec init` при найденном CLI и ссылкой без него; запуск через панель запуска; тесты «CLI установлен», «CLI не установлен»
- [ ] 3.2 «Открыть пример»: копирование `upstream-sample` в каталог данных и открытие; тест
- [ ] 3.3 Проверка инструментов на первом запуске со ссылками на установку; тест «нет агента»

## 4. Доступность

- [ ] 4.1 `Semantics` у строк таблицы, индикаторов и кнопок действий; тест «строка change'а» через `SemanticsTester`
- [ ] 4.2 Порядок фокуса и клавиатурная навигация по пунктам и экранам; тест «обход экранов»
- [ ] 4.3 Проверка на масштабе 150 % ключевых экранов; исправление переполнений; тест
