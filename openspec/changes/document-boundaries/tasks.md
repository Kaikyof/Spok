## 1. Границы в коде

- [ ] 1.1 Helper'ы `_map`/`_list` в источнике файлов, перевод девяти мест из `finding-recmeet-spec.md`; тесты на кривой YAML для каждого места; тест «group строкой»
- [ ] 1.2 Чтение `store:` в `config.yaml`, строка разбора и объяснение на экране группы с кнопкой выбора каталога; локализация; тест «конфиг-указатель»
- [ ] 1.3 Диагностика вложенных каталогов по признакам upstream (`proposal.md`, `tasks.md`, `design.md`, `.openspec.yaml`), `archive` исключён; тест «спека RecMeet» на фикстуре с двумя уровнями

## 2. Документы

- [ ] 2.1 README: третья спека в «Проверено на», таблица «что читается откуда» с `.openspec.yaml`, `apply.tracks`, встроенной схемой, раздел «Расширения»; проверить, что описание совпадает с поведением на трёх фикстурах
- [ ] 2.2 `platform-console-master.md`: часть 4.8 — уточнённый принцип «объяснять то, что спека обещает»; часть 8 — закрыть «трекер и хостинг прибиты», переформулировать «не-openspec спека»; состояние реализации обновить
- [ ] 2.3 `feature-plan.md` §13 → «Расширения», §1 — статусы только при улике трекера; `design-plan.md` очередь 3 — раздел «Расширения»; `finding-recmeet-spec.md` — пометить вариант A сделанным
