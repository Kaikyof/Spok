## 1. Источник

- [ ] 1.1 `OpenSpecCliSource` с поиском бинаря, вызовами `list`, `status`, `validate`, `schema which` (`--json`, таймаут 15 с, `CommandLog`); тесты на заглушке процесса с образцами JSON из upstream 1.13.2
- [ ] 1.2 Расширение `openspec-cli`: улика — бинарь, гейт `runtime`; тест «машина без CLI»

## 2. Использование

- [ ] 2.1 Бейдж валидации в группе и карточке; локализация; тест «change без дельт и без skip_specs»
- [ ] 2.2 `SchemaResolver`: источник CLI первым при активном расширении; тест «пользовательская схема»
- [ ] 2.3 Сверка счёта задач и предупреждение; тест «парсер отстал от upstream»
- [ ] 2.4 Версия CLI в разборе спеки, предупреждение при < 1.13; смоук печатает версию
