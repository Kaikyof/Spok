## 1. Спецификации проекта

- [ ] 1.1 `DocNodeKind.specs`, построение узла из `openspec/specs/**` с заголовком из H1; тесты «клон upstream» (на фикстуре — два каталога) и «спецификаций нет»
- [ ] 1.2 `docs_screen.dart`: узел рендерится после групп и перед архивом, раскрывается по каталогам; локализация заголовка; виджет-тест открытия `spec.md`

## 2. Дельты change'а

- [ ] 2.1 `_changeDocs`: раскрытие масок в строки с относительным путём и пометкой операции по заголовкам; серая строка при отсутствии файлов у активного change'а; тесты «change с двумя дельтами» и «архивный change»
- [ ] 2.2 Прогнать эталоны avtoto и avelacom: дерево отличается только новым узлом
