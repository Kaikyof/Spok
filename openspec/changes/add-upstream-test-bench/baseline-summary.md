# Эталоны «до» — сводка (26.09.2026)

Сняты `tool/compare_smoke.sh <spec> --save` на машине разработчика до правок
кода чтения спеки. Файлы лежат локально в `test/fixtures/baseline/`, в
репозиторий не входят. Здесь — только сводка для сверки с README
(«Проверено на двух спеках»).

| | avtoto | avelacom |
|---|---|---|
| каталог | `~/avtoto-platfrom` (имя с опечаткой, как у репозитория GitLab) | `~/avelacom-platform` |
| группировка | `sprintDir`: 3 спринта (7, 6 и 0 change'ей) + 1 вне групп | `masterDoc`: 1 мастер-спека (0 change'ей) + 1 вне групп |
| стеки | ios, android — ветки `features/<sprint>` по стекам, сборки у двух спринтов | один |
| фичи | builds, handoff, mergeRequests, chat, multiStack — все доступны | все пять недоступны, с чеклистами |
| команды | 38 | 23 |
| проблемы окружения | 0 | 6 |
| строк эталона | 93 | — |

Совпадает с README: avtoto — спринты, два стека, сборки, мессенджер;
avelacom — мастер-спеки, один стек, без сборок и MR.

Что учесть при сравнении после этапов 1–2: у avelacom мастер-спека с нулём
change'ей и один change вне групп — это состояние спеки на 26.09, а не
дефект чтения; появление строк «стадия», «источник схемы» и «расширения»
с реальными значениями — ожидаемая разница, всё остальное — регрессия.

Пересъём: после правки нормализации в `compare_smoke.sh` (счётчики
требований больше не заменяются на N) оба эталона нужно снять заново.
