## 1. Пакеты и граница

- [ ] 1.1 Создать `packages/spok_core` и перенести `lib/domain`, `lib/data` ядра, `lib/core` одним коммитом `git mv` без правок; приложение подключает пакет по `path`; `flutter analyze` и `flutter test` зелёные в корне
- [ ] 1.2 Перенести `lib/extensions/*` в `packages/spok_ext_*` с зависимостью на ядро; `main.dart` регистрирует пакеты; эталоны avtoto и avelacom без изменений
- [ ] 1.3 `tool/check_core_boundary.dart` (импорты `spok_ext_*`, слова `redmine|gitlab|mattermost` вне `test/fixtures`) и его тест на нарочно испорченном файле; добавить в `linux.yml` вместе с отдельным `analyze`/`test`/`dart format --set-exit-if-changed` в `packages/spok_core`
- [ ] 1.4 Флаг `--dart-define=SPOK_EXTENSIONS` в точке регистрации (по умолчанию `all`); тест «сборка только ядра» через `bool.fromEnvironment` в виджет-тесте

## 2. Локализация

- [ ] 2.1 `l10n.yaml` и `.arb` в каждом пакете; приложение собирает делегаты; проверить на первом пакете, что классы делегатов не конфликтуют; строки с именами систем переезжают в пакеты расширений, ядро — с `{tracker}`
- [ ] 2.2 `app_en.arb` для ядра и каждого расширения (черновик машинным переводом, вычитка по терминам change · spec · stage · tracker · extension); `supportedLocales: [en, ru]`, локаль из системы; виджет-тесты «английская система» и «русская система»
- [ ] 2.3 Тест паритета ключей между локалями всех пакетов; проверить, что он падает на нарочно удалённом ключе
- [ ] 2.4 Проверить, что описания команд и вывод агента не переводятся (тест «английская команда в русском интерфейсе»)

## 3. Следы команды и идентичность

- [ ] 3.1 Удалить `AVTOTO_PLATFORM_DIR` и типовые пути из `locate()` и `AppConfigSource` (`SPEC_PLATFORM_DIR` и конфиг остаются); `PlatformConsole` — только миграция каталога в `AppPaths`; тест миграции
- [ ] 3.2 Решение команды по названию «Spok» (проверка коллизий) и идентификатору `ru.webant.spok` — зафиксировать в `docs/adr/0001-identity.md`
- [ ] 3.3 «О приложении»: версия из `package_info_plus`, лицензии зависимостей через `LicenseRegistry`, ссылка на репозиторий; виджет-тест открытия диалога

## 4. Репозиторий

- [ ] 4.1 `LICENSE` (MIT), `README.md` на английском со скриншотами, «works with OpenSpec ≥ 1.13», таблицей «что читается откуда», разделом «Extensions» и заявлением о приватности; `README.ru.md`; проверить, что команды из README выполняются
- [ ] 4.2 `CONTRIBUTING.md` (сборка, тесты, смоуки на трёх фикстурах, правила стиля, чеклист адаптера, переводы), `CODE_OF_CONDUCT.md`, `SECURITY.md` (секреты, `--permission-mode`), `MAINTAINERS.md`, `CHANGELOG.md` (Keep a Changelog) с первым тегом `v0.1.0`
- [ ] 4.3 Шаблоны issue (bug, feature, «my spec is not recognized» с просьбой приложить вывод смоука) и PR, `CODEOWNERS`, `dependabot.yml` для pub и GitHub Actions; проверить, что шаблон появляется при создании issue
- [ ] 4.4 `ARCHITECTURE.md` и `ROADMAP.md` по-английски; русские документы в `docs/ru/`; внутренние ссылки (`gitlab.core.pl.webant.ru`, артефакты `claude.ai`, Penpot) убраны или макеты опубликованы; `grep` по репозиторию не находит внутренних хостов
- [ ] 4.5 `gitleaks detect` на полном клоне до первого публичного пуша; при находке — переписать историю; результат зафиксировать в PR
